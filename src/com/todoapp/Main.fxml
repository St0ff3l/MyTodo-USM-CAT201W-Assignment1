<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>

<BorderPane xmlns="http://javafx.com/javafx/25"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.todoapp.MainController"
            prefWidth="1000" prefHeight="650">

    <!-- 顶部：菜单栏 + 搜索框 -->
    <top>
        <VBox spacing="6">
            <MenuBar>
                <menus>
                    <Menu text="File"/>
                    <Menu text="Edit"/>
                    <Menu text="Help"/>
                </menus>
            </MenuBar>

            <HBox spacing="8" style="-fx-padding: 4 8;">
                <TextField fx:id="searchField" promptText="搜索日程..." HBox.hgrow="ALWAYS"/>
                <Button fx:id="filterBtn" text="筛选"/>
            </HBox>
        </VBox>
    </top>

    <!-- 左侧分类栏 -->
    <left>
        <VBox spacing="8" prefWidth="160" style="-fx-padding: 10;">
            <Button fx:id="btnToday" text="📅 今日"/>
            <Button fx:id="btnImportant" text="⭐ 重点"/>
            <Button fx:id="btnAll" text="📋 全部"/>
            <Button fx:id="btnFinished" text="✅ 已完成"/>
            <Button fx:id="btnPending" text="🕒 未完成"/>
        </VBox>
    </left>

    <!-- 中心：任务显示区 -->
    <center>
        <ListView fx:id="taskList"/>
    </center>

    <!-- 底部：添加栏 -->
    <bottom>
        <HBox spacing="8" style="-fx-padding: 10;" alignment="CENTER_LEFT">
            <TextField fx:id="quickAddField" promptText="添加新任务..." HBox.hgrow="ALWAYS"/>
            <Button fx:id="quickAddBtn" text="➕ 快速添加"/>
            <Button fx:id="detailAddBtn" text="📝 详细添加"/>
        </HBox>
    </bottom>

</BorderPane>